Host *
   # Remote hosts will typically terminate connections after 60 seconds of idleness
   ServerAliveInterval 60
   # Only enable compression for systems connected over very slow links
   Compression no
   # Disable X11 forwarding except when specifically requested
   ForwardX11 no
